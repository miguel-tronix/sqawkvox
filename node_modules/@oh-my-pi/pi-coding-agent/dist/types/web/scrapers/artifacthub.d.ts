import type { SpecialHandler } from "./types.js";
/**
 * Handle Artifact Hub URLs via API
 * Supports Helm charts, OLM operators, Falco rules, OPA policies, etc.
 */
export declare const handleArtifactHub: SpecialHandler;
